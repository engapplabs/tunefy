package com.abuarquemf.tunefy.desktopapp.streamhandler

class UserResourcesHandler {

    /**
     * It gets user account state on server
     * and brings it to local machine.
     *
     */
    fun retrieveUserResourcesHandler() {

    }

    /**
     * It gets a tune id and retrieves the propper
     * tune from server.
     *
     */
    fun retrieveTune(tuneId: Long) {

    }
}
